.env
venv/
__pycache__/
*.pyc
.DS_Store

# Secrets — never commit
credentials.json
token.json*

# Local agent config / caches
.claude/
.scheduled_tasks.lock

# QA cache + rebuild checkpoints/logs (local-only)
.deck_qa/
.full_rebuild_checkpoint.json
.full_rebuild_log.txt
.svg_rebuild_checkpoint.json
.svg_rebuild_log.txt

# Failed-attempt artifacts + local composed manifest cache
*.attempt_*.failed.json
.composed_manifest.json

# One-off remediation snapshots (local-only revert backups)
language/_*_backup/
language/**/_live_before/
_live_before/
